# tenx-helm-trusted-charts

Vendored third-party Helm charts: reviewed as source, optionally patched,
republished to `oci://ghcr.io/tenxprotocols/helm-trusted-charts/<chart>`.

Bootstrap in progress — see docs in a later commit.
